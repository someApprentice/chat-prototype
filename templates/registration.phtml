    <div class="container">
        <form method="post" name="registration" action="registration.php" class="authorization-form">
            <label>
                Login
                <input type="text" name="login" pattern="\D{1, 20}" required value="<?= (isset($post['login'])) ? htmlspecialchars($post['login'], ENT_QUOTES) : '' ?>">
                <span class="form-error">
                    <?= (isset($errors) and array_key_exists('login', $errors) ? $errors['login'] : '' ) ?>
                </span>
            </label>

            <label>
                Name
                <input type="text" name="name" pattern="\D{1,20}" required value="<?= (isset($post['name'])) ? htmlspecialchars($post['name'], ENT_QUOTES) : '' ?>">
                <span class="form-error">
                    <?= (isset($errors) and array_key_exists('name', $errors) ? $errors['name'] : '' ) ?>
                </span>
            </label>

            <label>
                Password
                <input type="password" name="password" pattern="(.){6,128}" required>
                <span class="form-error">
                    <?= (isset($errors) and array_key_exists('password', $errors) ? $errors['password'] : '' ) ?>
                </span>
            </label>
            <label>
                Retry password
                <input type="password" name="retryPassword" pattern="(.){6,128}" required>
                <span class="form-error">
                    <?= (isset($errors) and array_key_exists('retryPassword', $errors) ? $errors['retryPassword'] : '' ) ?>
                </span>
            </label>

            <input type="submit" name="input" value="Sign Up">
        </form>
    </div>